{
  "name": "nexus",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "web",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=web"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.63",
    "@tavily/core": "^0.7.3",
    "ai": "^6.0.182"
  }
}
