{
  "name": "nexus",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*", "web", "cli"],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=web"
  }
}
